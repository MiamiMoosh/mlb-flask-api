import React from 'react';
import { Text, View, StyleSheet, Image } from 'react-native';
import * as Animatable from 'react-native-animatable';
import cardImage from '../assets/defaultCard.png';

export default function AnimatedCard({ name, animation }) {
  return (
     <Animatable.View animation={animation} duration={800} style={styles.card}>
      <Image source={imageSource ? { uri: imageSource } : cardImage} style={styles.image} resizeMode="cover" />
      <View style={styles.overlay}>
        <Text style={styles.text}>{name}</Text>
      </View>
    </Animatable.View>
  );
}

const styles = StyleSheet.create({
  card: {
    width: 80,
    height: 100,
    margin: 8,
    borderRadius: 8,
    overflow: 'hidden',
    elevation: 4,
    backgroundColor: '#f9f9f9'
  },
  image: {
    width: '100%',
    height: '100%',
  },
  overlay: {
    position: 'absolute',
    bottom: 0,
    backgroundColor: 'rgba(0,0,0,0.5)',
    width: '100%',
    padding: 4,
  },
  text: {
    color: 'white',
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 14
  }
});